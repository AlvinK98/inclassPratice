Sys.getlocale()
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
which(transcriptData$學號 == "edfeQ7fb8")
subsample$學期成績 >= 60 -> subsample$及格 # 1
install.packages("chinese.misc")
View(transcriptData)
which(transcriptData$學號 == "edfeQ7fb8")
transcriptData %>%
mutate(
subsample,
總修習學分數=as.factor(總修習學分數),
總實得學分數=as.factor(總實得學分數)
)
transcriptData %>%
mutate(
subsample,
總修習學分數=as.factor(總修習學分數),
總實得學分數=as.factor(總實得學分數)
)
(transcriptData$學號 == "edfeQ7fb8") -> logIndex
transcriptData[logIndex,] -> subsample
View(subsample)
mutate(
subsample,
學期成績=as.numeric(學期成績)
及格=as.factor(及格)
mutate(
subsample,
學期成績=as.numeric(學期成績),
及格=as.factor(及格)
) -> subsample2
mutate(
subsample,
學期成績=as.factor(學期成績),
及格=as.factor(及格)
) -> subsample2
mutate(
subsample,
學期成績=as.factor(學期成績),
及格=as.factor(及格)
) -> subsample2
mutate(
subsample,
學期成績=as.factor(學期成績),
及格=as.factor(及格)
%>%
select(總修習學分數,總實得學分數) %>%
summarise(
總修習學分數=sum(總修習學分數),
總實得學分數=sum(總實得學分數)
)
transcriptData$系別 <- str_sub(transcriptData$學號,5,6)
View(transcriptData)
?unique()
transcriptData$通識科目總表 <-  (transcriptData$學屆== "100") && (transcriptData$`必選修類別（必∕選∕通）`=="通")
subsample %>%
select(總修習學分數,總實得學分數) %>%
summarise(
總修習學分數=sum(總修習學分數),
總實得學分數=sum(總實得學分數)
)
subsample %>%
select(總修習學分數,總實得學分數) %>%
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
)
subsample %>%
select(總修習學分數,總實得學分數) %>%
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數 %in% subsample$及格)
)
mutate(
subsample,
學期成績=as.factor(學期成績),
及格=as.factor(及格)
) -> subsample2
subsample %>%
select(總修習學分數,總實得學分數) %>%
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數 %in% subsample$及格)
)
mutate(
subsample,
學期成績=as.factor(學期成績),
及格=as.factor(及格)
) -> subsample2
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
總修習學分數 <- summarise(subsample$學分數)
subsample$學期成績 >= 60 -> subsample$及格 # 1
總修習學分數=sum(subsample$學分數) # 2
總實得學分數=sum(subsample$學分數[subsample$及格]) #3
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
subsample2 <- summarise(subsample, 總修習學分數=sum(subsample$學分數),總實得學分數=sum(subsample$學分數[subsample$及格]))
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
subsample$總修習學分數=sum(subsample$學分數)
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
(transcriptData$學號 == "edfeQ7fb8") -> logIndex
transcriptData[logIndex,] -> subsample
subsample$學期成績 >= 60 -> subsample$及格 # 1
總修習學分數=sum(subsample$學分數) # 2
總實得學分數=sum(subsample$學分數[subsample$及格]) #3
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
subsample$總修習學分數=sum(subsample$學分數)
summarise()
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
(transcriptData$學號 == "edfeQ7fb8") -> logIndex
transcriptData[logIndex,] -> subsample
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
subsample$總修習學分數=sum(subsample$學分數)
summarise()
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
(transcriptData$學號 == "edfeQ7fb8") -> logIndex
transcriptData[logIndex,] -> subsample
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
subsample$總修習學分數=sum(subsample$學分數)
summarise()
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
總修習學分數=sum(subsample$學分數)
總實得學分數=sum(subsample$學分數[subsample$及格])
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
總修習學分數=sum(subsample$學分數)
總實得學分數=sum(subsample$學分數 %in% subsample$及格)
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
總修習學分數=sum(subsample$學分數)
總實得學分數=sum(subsample$學分數[subsample$及格])
transcriptData$通識科目總表 <-  (transcriptData$學屆== "100") && (transcriptData$`必選修類別（必∕選∕通）`=="通")
transcriptData$通識科目總表 <-  which(transcriptData$學屆== "100") && (transcriptData$`必選修類別（必∕選∕通）`=="通")
transcriptData$通識科目總表 <-  which(transcriptData$學屆== "100") && which(transcriptData$`必選修類別（必∕選∕通）`=="通")
View(transcriptData)
transcriptData$通識科目總表 <-  which(transcriptData$學屆 == "100") && which(transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$系別 <- str_sub(transcriptData$學號,5,6)
transcriptData$通識科目總表 <-  which(transcriptData$學屆 == "100") | which(transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <-  which(transcriptData$學屆 == "100") || which(transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <-  which(transcriptData$學屆 == "100") || which(transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <-  (transcriptData$學屆 == "100") || (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <-  (transcriptData$學屆 == "100") | (transcriptData$`必選修類別（必∕選∕通）` =="通")
View(transcriptData)
transcriptData$通識科目總表 <-  (transcriptData$學屆 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <-  (transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
unique(transcriptData$通識科目總表 <-  (transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通"))
unique %in% transcriptData$通識科目總表 <-  (transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
unique(transcriptData$通識科目總表 <-  (transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通"))
transcriptData$通識科目總表 <-  unique(transcriptData$科目名稱) %in% (transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- transcriptData$科目名稱[unique(transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")]
transcriptData$通識科目總表 <- which(transcriptData$科目名稱) unique(transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- unique (transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- unique [(transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")]
transcriptData$通識科目總表 <- unique (transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
x <- (transcriptData$系別== "ZT13") & (transcriptData$學年 == "104")
shannon(x)
x <- (transcriptData$科目名稱 == "ZT13") & (transcriptData$學年 == "104")
shannon(x)
x <- (transcriptData$科目名稱 == "ZT13") & (transcriptData$學年 == "104")
shannon(x)
shannon <- (transcriptData$系別)
x <- (transcriptData$科目名稱 == "ZT13") & (transcriptData$學年 == "104")
shannon(x)
shannon(transcriptData$系別) <- -sum(x*log(x))
x <- (transcriptData$科目名稱 == "ZT13") & (transcriptData$學年 == "104")
shannon(x)
subsample$學期成績 >= 60 -> subsample$及格 # 1
總修習學分數=sum(subsample$學分數) # 2
總實得學分數=sum(subsample$學分數[subsample$及格]) #3
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
View(subsample)
(transcriptData$學號 == "edfeQ7fb8") -> logIndex
transcriptData[logIndex,] -> subsample
subsample$學期成績 >= 60 -> subsample$及格 # 1
總修習學分數=sum(subsample$學分數) # 2
總實得學分數=sum(subsample$學分數[subsample$及格]) #3
subsample$及格 <- mutate(subsample,subsample$學期成績 >= 60)
summarise()
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
summarise(學號=...)
summarise(subsample$學號)
install.packages("summarytools")
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
(transcriptData$學號 == "edfeQ7fb8") -> logIndex
transcriptData[logIndex,] -> subsample
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
select(總修習學分數,總實得學分數) %>%
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
library(summarytools)
subsample %>%
summarise(總修習學分數,總實得學分數,學號)
library(summarytools)
subsample %>%
summarise(總修習學分數,總實得學分數)
library(summarytools)
subsample %>%
summarise(總修習學分數,總實得學分數,subsample$學號)
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
(transcriptData$學號 == "edfeQ7fb8") -> logIndex
transcriptData[logIndex,] -> subsample
subsample$學期成績 >= 60 -> subsample$及格 # 1
總修習學分數=sum(subsample$學分數) # 2
總實得學分數=sum(subsample$學分數[subsample$及格]) #3
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
select(總修習學分數,總實得學分數) %>%
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
library(summarytools)
subsample %>%
summarise(總修習學分數,總實得學分數,as.numeric(subsample$學號))
library(summarytools)
subsample %>%
summarise(總修習學分數,總實得學分數)
library(summarytools)
subsample %>%
dfsummarise(總修習學分數,總實得學分數)
library(summarytools)
subsample %>%
dfsummarise(.,總修習學分數,總實得學分數)
library(summarytools)
subsample %>%
dfSummary(總修習學分數,總實得學分數)
library(summarytools)
subsample %>%
dfSummary(subsample,總修習學分數,總實得學分數)
library(summarytools)
subsample %>%
dfSummary(學號，總修習學分數,總實得學分數)
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
summarise(
總修習學分數=as.numeric(總修習學分數),
總實得學分數=as.numeric(總實得學分數)
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
summarise(
總修習學分數=as.factor(總修習學分數),
總實得學分數=as.factor(總實得學分數)
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
subsample %>%
summarise(學號，總修習學分數,總實得學分數)
subsample %>%
summarise( `學號=...` ，總修習學分數,總實得學分數)
subsample %>%
summarise( `學號` ，總修習學分數,總實得學分數)
subsample %>%
summarise( subsample$學號 ，總修習學分數,總實得學分數)
totalCredits(subsample$學號) <- summary(subsample)
totalCredits(subsample$學號) <- summarise(subsample)
totalCredits(edfeQ7fb8) <- summarise(subsample)
totalCredits(subsample$學號= "edfeQ7fb8") <- summarise(subsample)
totalCredits(subsample$學號== "edfeQ7fb8") <- summarise(subsample)
totalCredits <- summarise(subsample)
totalCredits(subsample$學號== "edfeQ7fb8") <- summarise(subsample)
totalCredits <- summarise(subsample)
totalCredits(edfeQ7fb8)
View(totalCredits)
transcriptData$通識科目總表 <- unique (transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- unique (transcriptData$學年 == "100") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
View(transcriptData)
transcriptData$通識科目總表 <- unique (transcriptData$學年 == "100")& (transcriptData$學期 == "1") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- unique(transcriptData$學年 == "100")& unique(transcriptData$學期 == "1") & unique(transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- unique[(transcriptData$學年 == "100")& (transcriptData$學期 == "1") & (transcriptData$`必選修類別（必∕選∕通）` =="通")]
transcriptData$通識科目總表 <- (transcriptData$學年 == "100")& (transcriptData$學期 == "1") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- unique(transcriptData$通識科目總表)
transcriptData$通識科目總表 <- (transcriptData$學年 == "100")& (transcriptData$學期 == "1") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
unique(transcriptData)
transcriptData$通識科目總表 <- which(transcriptData$學年 == "100") & (transcriptData$學期 == "1") & (transcriptData$`必選修類別（必∕選∕通）` =="通")
unique(transcriptData)
transcriptData$通識科目總表 <- which(transcriptData$學年 == "100" +transcriptData$學期 == "1" + (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- which(transcriptData$學年 == "100" +transcriptData$學期 == "1" + (transcriptData$`必選修類別（必∕選∕通）` =="通"))
transcriptData$通識科目總表 <- which(transcriptData$學年 == "100" && transcriptData$學期 == "1") && (transcriptData$`必選修類別（必∕選∕通）` =="通")
unique(transcriptData)
transcriptData$通識科目總表 <- transcriptData$學年 == "100" %in% transcriptData$學期 == "1" %in% (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- transcriptData$學年 == "100" %>% transcriptData$學期 == "1" %>% (transcriptData$`必選修類別（必∕選∕通）` =="通")
transcriptData$通識科目總表 <- transcriptData$學年 = "100" %>% transcriptData$學期 = "1" %>% (transcriptData$`必選修類別（必∕選∕通）`=="通")
knitr::opts_chunk$set(echo = TRUE)
library(dplyr); library(stringr); library(lubridate); library(purrr)
library(readr)
transcriptData <- read_csv("https://raw.githubusercontent.com/tpemartin/github-data/master/transcript100_102.csv",
col_types = cols(
學期成績="n"
))
(transcriptData$學號 == "edfeQ7fb8") -> logIndex
transcriptData[logIndex,] -> subsample
subsample$學期成績 >= 60 -> subsample$及格 # 1
總修習學分數=sum(subsample$學分數) # 2
總實得學分數=sum(subsample$學分數[subsample$及格]) #3
subsample %>%
mutate(
及格=subsample$學期成績 >= 60
)
summarise(
總修習學分數=sum(subsample$學分數),
總實得學分數=sum(subsample$學分數[subsample$及格])
transcriptData$通識科目總表 <- transcriptData$學年 = "100" | transcriptData$學期 = "1" | (transcriptData$`必選修類別（必∕選∕通）`=="通")
transcriptData$通識科目總表 <- (transcriptData$學年 = "100" | transcriptData$學期 = "1" | (transcriptData$`必選修類別（必∕選∕通）`=="通"))
